# 🚀 Xkeen UI — веб‑панель для XKeen (Keenetic / Entware)

**Xkeen UI** — лёгкая веб панель для роутеров **Keenetic** с установленным **Entware**, которая помогает управлять конфигурациями **Xray/Mihomo** и файлами **XKeen**, смотреть логи и выполнять команды — прямо из браузера.

> ⚠️ Панель рассчитана на домашнюю сеть. Старайся не публиковать её в интернет. Используй пароль и при необходимости ограничивай доступ (ACL/Firewall/VPN).

---

## ✨ Что нового (актуальные фишки)

- **Встроенная авторизация + CSRF-защита**
  - при первом запуске открывается мастер **Setup** (логин/пароль);
  - дальше — стандартный логин/логаут; для API POST/PUT/DELETE работает CSRF.

- **Локальный CodeMirror (без CDN)**
  - подсветка синтаксиса для **JSON/JSONC**, **YAML**, **Shell**;
  - удобный поиск/замена, полноэкранный режим, авто скобки и т.п.;
  - `jsonlint` тоже локально (проверка JSON).

- **Два терминала**
  1) **«Урезанный» терминал / команд раннер** — безопасный режим для запуска `xkeen` команд (и опционально произвольных команд).
  2) **Полноценный интерактивный PTY shell** (xterm.js + WebSocket) — как “настоящий SSH”, но в браузере.

- **Live логи Xray**
  - если доступны WebSocket’ы — “живой tail -f”;
  - если WebSocket недоступен — аккуратный **HTTP polling** (фоллбек).

- **Routing как JSONC**
  - можно хранить **комментарии** (редактируется `.jsonc`);
  - при сохранении панель убирает комментарии и пишет валидный `.json`.

---

## 🧩 Что умеет панель

### Xray
- Роутинг: `05_routing.json` (и “сырой” `05_routing.jsonc` с комментариями)
- Inbounds: `03_inbounds.json`
- Outbounds: `04_outbounds.json`
- Бэкапы в: `/opt/etc/xray/configs/backups`
- Логи:
  - `/opt/var/log/xray/error.log`
  - `/opt/var/log/xray/access.log`

### Mihomo
- Конфиг: `/opt/etc/mihomo/config.yaml`
- Шаблоны: `/opt/etc/mihomo/templates/*`
- Валидация синтаксиса (если установлен `mihomo` и включена проверка)

### XKeen
- Списки:
  - `/opt/etc/xkeen/port_proxying.lst`
  - `/opt/etc/xkeen/port_exclude.lst`
  - `/opt/etc/xkeen/ip_exclude.lst`
- Перезапуск `xkeen` (кнопка + авто‑рестарт по чекбоксам в UI)
- Команды `xkeen` (готовые кнопки + вывод результата)

---

## 📦 Установка (SSH)

### Вариант A — архив уже на роутере

```sh
cd /opt
tar -xzf xkeen-ui-routing.tar.gz
cd xkeen-ui
sh install.sh
```

### Вариант B — скачать релиз и поставить

```sh
cd /opt \
  && curl -fSL "https://github.com/umarcheh001/Xkeen-UI/releases/download/v1.2.9/xkeen-ui-routing.tar.gz" -o xkeen-ui-routing.tar.gz \
  && tar -xzf xkeen-ui-routing.tar.gz \
  && sh xkeen-ui/install.sh
```

### Что делает `install.sh`
- проверяет наличие **python3** в Entware и ставит при необходимости;
- ставит **Flask** (обязательно) и **gevent/gevent-websocket** (опционально);
  - на некоторых **mips/mipsel** роутерах gevent может не собраться — тогда панель работает без WebSocket (HTTP polling, PTY может быть недоступен);
- подбирает свободный порт (по умолчанию **8088**, затем **8091**, затем **8100–8199**);
- копирует панель в `/opt/etc/xkeen-ui`;
- ставит init скрипт `/opt/etc/init.d/S99xkeen-ui` и запускает сервис;
- при **первой установке** делает autobackup Xray конфигов в `/opt/etc/xray/configs/backups`.

---

## 🌐 Доступ к панели

```text
http://<IP_роутера>:<порт>/
```

Порт можно увидеть в логе:

- `/opt/var/log/xkeen-ui.log`

---

## 🔐 Первый запуск: Setup / Login

После установки открой URL панели — если учётка ещё не настроена, тебя перебросит на страницу **Setup**:
1) создай логин/пароль;
2) затем залогинься.

Файлы состояния/безопасности по умолчанию живут в:
- `/opt/etc/xkeen-ui/auth.json` (хеш пароля)
- `/opt/etc/xkeen-ui/secret.key` (секрет сессий)

Путь можно переопределить переменной окружения `XKEEN_UI_STATE_DIR`.

---

## 🧑‍💻 Управление сервисом через SSH

```sh
/opt/etc/init.d/S99xkeen-ui start
/opt/etc/init.d/S99xkeen-ui stop
/opt/etc/init.d/S99xkeen-ui restart
/opt/etc/init.d/S99xkeen-ui status
```

Удаление:

```sh
sh /opt/etc/xkeen-ui/uninstall.sh
```

Логи:
- панель: `/opt/var/log/xkeen-ui.log`
- debug WebSocket: `/opt/var/log/ws.log`
- перезапуски xkeen (UI): `/opt/etc/xkeen-ui/restart.log`

---

## 🖥️ Терминалы: как это работает

### 1) «Урезанный» терминал (Command Runner)
Этот режим полезен, когда нужен “безопасный запуск команд” без полноценного интерактивного шелла.

- **`xkeen` команды** запускаются как фоновые задачи (job):
  - `POST /api/run-command` → возвращает `job_id`
  - `GET /api/run-command/<job_id>` → статус/вывод/код завершения

- Можно передавать **stdin** (например, подтверждения/ввод), но это всё равно “одноразовый” запуск, не интерактивная TTY сессия.

- Запуск **произвольной команды** (поле `cmd`) управляется переменной окружения:
  - `XKEEN_ALLOW_SHELL=1` (по умолчанию включено)
  - `XKEEN_ALLOW_SHELL=0` — отключить произвольные команды, оставить только whitelisted `xkeen` flags

> Рекомендация: если панель доступна не только тебе — **выключи** произвольный шелл: `XKEEN_ALLOW_SHELL=0`.

---

### 2) Полноценный PTY терминал (Interactive Shell)
Это “настоящий” интерактивный терминал в браузере:

- фронт: **xterm.js** (локально в `static/xterm/`)
- бэкенд: **PTY** через `os.openpty()` + `subprocess.Popen()`
- транспорт: **WebSocket** `/ws/pty` (нужны `gevent` + `gevent-websocket`)

#### Безопасность PTY
Перед открытием PTY фронт запрашивает **одноразовый токен**:
- `POST /api/ws-token` → токен живёт 10–300 секунд и используется один раз
- затем подключение: `/ws/pty?token=...`

Если токен неверный/просроченный — сервер вернёт `{"type":"error","message":"unauthorized"}` и закроет WS.

#### Reconnect без потери вывода
PTY сессия умеет “переподключение”:

- сервер создаёт `session_id` и буферизует вывод с **seq номерами**;
- браузер хранит `session_id` и `last_seq` в `sessionStorage`;
- при реконнекте клиент передаёт `session_id` + `last_seq`, а сервер “досылает” пропущенный вывод.

#### Detach / Kill / Signals
В UI есть удобные действия:
- **Detach** — закрыть оверлей и WS, но **не убивать** shell (можно вернуться);
- **Kill session** — завершить PTY на сервере;
- меню сигналов: `SIGINT`, `SIGTERM`, `SIGKILL`, `SIGHUP`, `SIGQUIT`;
- resize терминала отправляется на сервер (и пробрасывается `SIGWINCH`).

#### Параметры PTY через env
- `XKEEN_PTY_MAX_BUF_CHARS` — размер буфера вывода (по умолчанию `65536`)
- `XKEEN_PTY_IDLE_TTL_SECONDS` — время жизни “отсоединённой” сессии (по умолчанию `1800`, т.е. 30 минут)

---

## 🧰 Полезные переменные окружения

- `XKEEN_UI_STATE_DIR` — где хранить состояние UI (auth/secret/restart.log и т.д.)
- `XKEEN_ALLOW_SHELL` — разрешить произвольные команды в “урезанном” терминале (`1`/`0`)
- `XKEEN_XRAY_LOG_TZ_OFFSET` — сдвиг времени для логов (часы), по умолчанию `3`
- `XKEEN_RESTART_LOG_FILE` — путь к restart логу

---

## 🛠️ Troubleshooting

### В панели нет PTY терминала / не подключается
1) Проверь, что установлены модули:
```sh
/opt/bin/python3 -c "import gevent, geventwebsocket; print('ok')"
```
2) На **mips/mipsel** сборка gevent может не пройти — это ожидаемо. Тогда:
- PTY может быть недоступен,
- логи будут работать через HTTP polling.

### “unauthorized” при подключении PTY
- Обычно это просроченный/одноразовый токен или ты не залогинен.
- Открой панель заново, залогинься и попробуй ещё раз.

### Сбросить логин/пароль
Удалить файл авторизации (после остановки сервиса), затем зайти — панель снова откроет Setup:

```sh
/opt/etc/init.d/S99xkeen-ui stop
rm -f /opt/etc/xkeen-ui/auth.json
/opt/etc/init.d/S99xkeen-ui start
```

---

## 📝 Примечания
- Все фронтенд библиотеки (CodeMirror, xterm.js) **встроены локально** — панель не зависит от внешних CDN.
- Проект ориентирован на Keenetic/Entware, но бэкенд аккуратно выбирает writable директории и может запускаться в dev среде.

---

![xkeen-ui-demo](https://github.com/user-attachments/assets/89e55119-d875-4b9b-af94-4bfc0d2a2da7)

