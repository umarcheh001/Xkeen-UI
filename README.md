

# 🚀 Xkeen UI — веб‑панель для XKeen

Панель для роутеров **Keenetic (Entware)**, которая управляет:

- файлами Xray: `03_inbounds.json`, `04_outbounds.json`, `05_routing.json`;
- конфигом **Mihomo** (`/opt/etc/mihomo/config.yaml`) и его шаблонами;
- текстовыми файлами XKeen: `port_proxying.lst`, `port_exclude.lst`, `ip_exclude.lst`;
- логами Xray;
- запуском/остановкой **xkeen** и его CLI командами;
- экспортом/импортом конфигураций (локально и через GitHub).

---

## 📦 Установка (SSH)

### Если архив уже на роутере

```sh
cd /opt
tar -xzf xkeen-ui-routing.tar.gz
cd xkeen-ui
sh install.sh
```

### Онлайн установка

```sh
cd /opt && curl -fSL "https://github.com/umarcheh001/Xkeen-UI/releases/download/v1.0.5/xkeen-ui-routing.tar.gz" -o xkeen-ui-routing.tar.gz && tar -xzf xkeen-ui-routing.tar.gz && sh xkeen-ui/install.sh
```

Скрипт:

- найдёт или установит `python3` в Entware;
- выберет порт панели: 8088 → 8091 → свободный 8100–8199;
- сохранит порт в `app.py` и `/opt/var/log/xkeen-ui.log`;
- при первой установке создаст автобэкапы `03_inbounds.json`, `04_outbounds.json`, `05_routing.json`;
- установит `/opt/etc/init.d/S99xkeen-ui` и запустит сервис.

---

## 🌐 Доступ к панели

```text
http://<IP_роутера>:<порт>
```

Пример: `http://192.168.1.1:8088`.

Текущий порт можно посмотреть в `/opt/var/log/xkeen-ui.log`.

---

## 🔧 Управление панелью через SSH

```sh
/opt/etc/init.d/S99xkeen-ui start
/opt/etc/init.d/S99xkeen-ui stop
/opt/etc/init.d/S99xkeen-ui restart
/opt/etc/init.d/S99xkeen-ui status
```

Удаление:

```sh
sh /opt/etc/xkeen-ui/uninstall.sh
```

Журналы:

- панель: `/opt/var/log/xkeen-ui.log`
- перезапуски xkeen: `/opt/etc/xkeen-ui/restart.log`

---

## 🖥 Основные возможности

### 1. Routing (`05_routing.json`)

- JSON редактор.
- Кнопки:
  - **Save routing**;
  - **Format JSON**;
  - **Backup**;
  - **Restore autobackup**;
  - **Export / Import bundle** (локальный файл).

### 2. Inbounds (`03_inbounds.json`)

- Режимы:
  - `Mixed` — TCP Redirect, UDP TProxy (по умолчанию);
  - `TProxy` — всё через TProxy;
  - `Redirect` — всё через Redirect.
- Кнопки:
  - **Save inbounds**;
  - **Backup 03_inbounds.json**;
  - **Restore inbounds** (из автобэкапа).
- Отдельный чекбокс авторестарта `xkeen` для inbounds.

### 3. Outbounds (`04_outbounds.json`)

- Поле для вставки **VLESS‑ссылки**.
- Кнопки:
  - **Save VLESS**;
  - **Backup 04_outbounds.json**.

### 4. Бэкапы Xray

Каталог: `/opt/etc/xray/configs/backups`.

В UI — список бэкапов `03_inbounds.json`, `04_outbounds.json`, `05_routing.json` с восстановлением и удалением в один клик.

---

### 5. Mihomo (`config.yaml` и шаблоны)

- Редактор `/opt/etc/mihomo/config.yaml`.
- Работа с шаблонами в `/opt/etc/mihomo/templates`:
  - загрузка шаблона в редактор;
  - сохранение текущего конфига как шаблона.
- Кнопки сохранения и перезапуска `xkeen`.

---

### 6. Порты и исключения XKeen

Редактирование:

- `/opt/etc/xkeen/port_proxying.lst`
- `/opt/etc/xkeen/port_exclude.lst`
- `/opt/etc/xkeen/ip_exclude.lst`

Каждый блок: кнопка **Save** (по умолчанию с авто‑рестартом `xkeen`).

---

### 7. Live логи Xray

- выбор `error.log` / `access.log`;
- разовый просмотр и автообновление;
- фильтр по строке;
- кнопки:
  - **Enable logs** / **Disable logs**;
  - **Clear log view**;
  - **Truncate log files**;
  - **Copy to clipboard**.

---

### 8. Команды XKeen и мини терминал

Вкладка **КОМАНДЫ XKeen**:

- готовые кнопки для запуска CLI команд `xkeen` (установка, обновление, бэкапы, работа с портами и т.д.);
- мини‑терминал с вводом в stdin и выводом результата;
- журнал перезапусков `restart.log` с кнопками очистки и копирования.

Глобальный чекбокс **Auto restart xkeen** управляет тем, будут ли операции сохранения автоматически перезапускать `xkeen`.

---

### 9. Экспорт / импорт конфигураций

#### Локальный файл

- **Export bundle** — скачивает JSON bundle (все пользовательские Xray‑конфиги и `.lst` файлы XKeen).
- **Import bundle** — загружает и разворачивает этот bundle на роутере.

#### GitHub‑репозиторий

- **Upload to repo** — отправка bundle на конфиг сервер, который:
  - создаёт папку `<тег>_XXXXXX` в `configs/` репозитория;
  - пишет туда `meta.json` и `bundle.json`;
  - обновляет `configs/index.json`.
- **Configs catalog** — загрузка списка доступных конфигов из `configs/index.json` на GitHub и импорт выбранного.


Репозиторий настраивается переменными окружения:

- `XKEEN_GITHUB_OWNER`
- `XKEEN_GITHUB_REPO`
- `XKEEN_GITHUB_BRANCH`
- `XKEEN_GITHUB_REPO_URL` (опционально, ссылка в UI).

<img width="876" height="1323" alt="Снимок экрана 2025-11-24 022323" src="https://github.com/user-attachments/assets/d11f814f-469b-4d7e-987f-cc608f2af625" />
