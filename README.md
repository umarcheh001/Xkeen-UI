<img width="974" height="1319" alt="Снимок экрана 2025-11-20 183058" src="https://github.com/user-attachments/assets/579921da-e2a3-4492-9b03-58da68a8e771" />

# 🚀 Xkeen UI — Панель управления Xray / Mihomo для Keenetic

Полнофункциональная вебпанель для роутеров **Keenetic**, включающая:
- авто‑подбор порта при конфликте,
- автобэкап файлов конфигурации при первой установке,
- ручные и автоматические бэкапы трёх ключевых файлов:
  - `03_inbounds.json`
  - `04_outbounds.json`
  - `05_routing.json`
- восстановление любого файла из автобэкапа или последнего ручного бэкапа,
- удобный UI с кнопками управления,
- управление сервисами через SSH.

---

# 📦 Установка через SSH

Ручная установка:

Загрузите архив на устройство в каталог `/opt` (например через WinSCP), затем выполните:

```sh
cd /opt && tar -xzf xkeen-ui-routing.tar.gz && cd xkeen-ui && sh install.sh
```
Онлайн установка:
```sh
cd /opt && URL=$(curl -s https://api.github.com/repos/umarcheh001/Xkeen-UI/releases/latest | grep browser_download_url | grep '.tar.gz"' | head -n 1 | cut -d '"' -f 4) && curl -L "$URL" -o xkeen-ui-latest.tar.gz && tar -xzf xkeen-ui-latest.tar.gz && cd xkeen-ui && sh install.sh
```

или:
```sh
curl -sL https://raw.githubusercontent.com/umarcheh001/Xkeen-UI/main/install_xkeen_from_github.sh \
  | tr -d '\r' \
  | sh
```

Скрипт:

1. проверит, свободен ли порт **8088**;
2. если порт занят, переключит на **8091**;
3. если и 8091 занят — выберет случайный свободный порт;
4. создаст автобэкапы всех конфигов при первой установке;
5. выведет конечный URL панели в консоль.

---

# 🌐 Адрес панели

После установки скрипт покажет:

```
Панель доступна по адресу:
http://<IP-адрес-роутера>:<порт>
```

Например:

```
http://192.168.1.1:8088
```

---

# 🔧 Управление через SSH

## ▶️ Запуск панели

```sh
/etc/init.d/S99xkeen-ui start
```

## ⏹ Остановка панели

```sh
/etc/init.d/S99xkeen-ui stop
```

## 🔄 Перезапуск панели

```sh
/etc/init.d/S99xkeen-ui restart
```

## 🧪 Проверка, что сервис работает

```sh
ps | grep xkeen
netstat -tlnp | grep python
netstat -tlnp | grep 8088
```

---

# 🛠 Что хранится в /opt/xkeen-ui

```
/opt/xkeen-ui/
 ├── app.py                    — backend (Flask)
 ├── static/main.js           — логика UI
 ├── templates/panel.html     — интерфейс панели
 ├── install.sh               — авто‑установка и автобэкапы
 └── backups/                 — каталог всех бэкапов
```

---

# 🖥 Возможности UI

В панели теперь есть полный набор действий для каждого файла:

---

## 🛂 03_inbounds.json

Кнопки:

- 💾 Сохранить режим inbounds  
- 📦 Создать бэкап inbounds  
- ↩️ Восстановить inbounds из *последнего ручного бэкапа*  
- ↩️ Восстановить inbounds из *автобэкапа*  

API:

```
POST /api/backup-inbounds
POST /api/restore-inbounds-last
POST /api/restore-auto-inbounds
```

---

## 🌐 04_outbounds.json

Кнопки:

- 💾 Сохранить VLESS ссылку  
- 📦 Создать бэкап outbounds  
- ↩️ Восстановить outbounds из *последнего ручного бэкапа*  
- ↩️ Восстановить outbounds из *автобэкапа*  

API:

```
POST /api/backup-outbounds
POST /api/restore-outbounds-last
POST /api/restore-auto-outbounds
```

---

## 🛰 05_routing.json

Кнопки:

- 💾 Сохранить  
- 📦 Создать бэкап  
- ↩️ Восстановить из *автобэкапа*  

API:

```
POST /api/backup
POST /api/restore-auto-routing
```

---

# 🐛 Ошибка 403 Forbidden

Самые частые причины:

### 1. Панель запущена на порту, занятом lighttpd  
Проверка:

```sh
netstat -tlnp | grep 8088
```

Если увидите:

```
tcp 0 0 0.0.0.0:8088 ... LISTEN 1503/lighttpd
```

значит порт занят webadmin Keenetic.

🔥 **install.sh автоматически решает это**, выбирая другой свободный порт.

---

# 🗂 Каталог бэкапов

Все файлы хранятся здесь:

```
/opt/etc/xray/configs/backups
```

Типы файлов:

- `*-auto-backup-*.json` — автобэкапы  
- `03_inbounds-*.json` — ручные бэкапы inbounds  
- `04_outbounds-*.json` — ручные бэкапы outbounds  
- `05_routing-*.json` — ручные бэкапы routing  

---

# 🧱 Как работает авто‑подбор порта

install.sh:

1. проверяет порт 8088  
2. если занят → проверяет 8091  
3. если занят → ищет случайный свободный порт  
4. сохраняет его в `/opt/xkeen-ui/port.txt`  
5. использует для запуска Flask‑панели  

---

# 🧹 Удаление панели

```sh
/etc/init.d/S99xkeen-ui stop
rm -rf /opt/xkeen-ui
```
