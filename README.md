# 🚀 Xkeen UI — веб‑панель для XKeen на Keenetic (Entware)

**Xkeen UI** — лёгкая, но функциональная веб панель для роутеров **Keenetic** с установленным **Entware**.  
Позволяет управлять конфигами **Xray / Mihomo**, сервисами **XKeen**, смотреть логи, выполнять команды, а также работать с файлами через встроенный **двухпанельный файловый менеджер** — прямо из браузера.

> ⚠️ Панель рассчитана на домашнюю сеть. Не публикуй её в интернет. Используй пароль и при необходимости ограничивай доступ (ACL / Firewall / VPN).

---

## ✨ Что умеет панель

### 🔐 Безопасность
- **Setup мастер** при первом запуске (создание логина/пароля).
- **Login/Logout**.
- **CSRF защита** для mutating API запросов (POST/PUT/DELETE/PATCH).

---

### 🧩 Xray / Mihomo / XKeen
**Xray**
- Редактирование конфигов:
  - `03_inbounds.json`
  - `04_outbounds.json`
  - `05_routing.json` (+ поддержка `05_routing.jsonc` с комментариями)
- Автобэкапы в: `/opt/etc/xray/configs/backups`
- Логи:
  - `/opt/var/log/xray/error.log`
  - `/opt/var/log/xray/access.log`

**Mihomo**
- Конфиг: `/opt/etc/mihomo/config.yaml`
- Шаблоны: `/opt/etc/mihomo/templates/*`
- (Опционально) генерация/проверка конфигов через UI (если компонент доступен в сборке)

**XKeen**
- Списки:
  - `/opt/etc/xkeen/port_proxying.lst`
  - `/opt/etc/xkeen/port_exclude.lst`
  - `/opt/etc/xkeen/ip_exclude.lst`
- Управление сервисом (Start/Stop/Restart) и быстрые команды из UI.

---

### 📜 Live‑логи Xray (в реальном времени)
- “живой tail” через **WebSocket**, если доступно;
- **HTTP polling** как надёжный fallback, если WebSocket недоступен;
- кнопка **Fullscreen** для карточки логов.

---

### 🖥️ Терминал
В панели есть два режима:

1) **Command Runner** (“урезанный терминал”)  
   Запускает команды как задачи (jobs) и возвращает вывод/код завершения.  
   Произвольные команды можно запретить через `XKEEN_ALLOW_SHELL=0`.

2) **Интерактивный PTY терминал** (xterm.js + WebSocket)  
   По сути “SSH в браузере”: resize, detach/kill, сигналы (SIGINT/SIGTERM/…), переподключение.

> На некоторых mips/mipsel сборках `gevent` может не собраться — тогда WebSocket функции (PTY/Live лог) могут работать в ограниченном режиме или через fallback.

---

### 📁 Файловый менеджер (двухпанельный)
- Две панели (Left/Right), быстрые операции между панелями.
- Операции **Copy / Move / Delete** выполняются как **jobs с прогрессом** (и возможностью отмены).
- **Корзина** для локальной ФС: удаление “в корзину”, восстановление, очистка корзины.
- **Архивирование и разархивирование**:
  - создание архивов `.zip` / `.tar.gz` (диалог имени и формата);
  - распаковка `.zip` / `.tar.gz` в выбранную папку.
- **Свойства** (права/размер/метаданные). Для папок умеет считать **реальный размер** (через `du`, с fallback логикой).
- Кнопка **«Терминал здесь»** — открыть терминал в текущей директории.
- Поддержка **локальных путей** (в т.ч. `/tmp/mnt`) и **удобных меток томов** (Keenetic mounts).

#### 🌍 Remote File Manager (SFTP/FTP/FTPS)
- Опциональная работа с удалёнными хостами через **lftp**.
- Сессии живут в RAM (без сохранения паролей на диск).
- Включается/выключается переменной `XKEEN_REMOTEFM_ENABLE`.

---

### 🛠️ DevTools (тема / UI / диагностика)
- **Тема и CSS переменные (глобально)**: цвета, эффекты, геометрия, прозрачности.
- Отдельные секции настроек:
  - **Terminal** (внешний вид терминала),
  - **CodeMirror** (темы/цвета/прозрачности редактора),
  - **File Manager** (прозрачность/панели/кнопки/UX опции).
- **Шапка / Брендинг**.
- **Layout твики** (в т.ч. управление подсказками горячих клавиш файлового менеджера).
- Логи панели и служебные инструменты (в зависимости от сборки).

---

## 📦 Установка (SSH)

### Вариант A — архив уже на роутере
```sh
cd /opt
tar -xzf xkeen-ui-routing.tar.gz
cd xkeen-ui
sh install.sh
```

### Вариант B — скачать из Releases и поставить
```sh
cd /opt \
  && curl -fL -o xkeen-ui-routing.tar.gz "https://github.com/umarcheh001/Xkeen-UI/releases/latest/download/xkeen-ui-routing.tar.gz" \
  && tar -xzf xkeen-ui-routing.tar.gz \
  && sh xkeen-ui/install.sh
```

### Что делает `install.sh`
- проверяет/ставит **python3** (Entware);
- ставит **Flask** (обязательно) и (опционально) **gevent / gevent-websocket** для WebSocket функций;
- проверяет/ставит **lftp** (для Remote File Manager);
- подбирает свободный порт (обычно **8088**, затем **8091**, затем диапазон **8100–8199**);
- устанавливает панель в `/opt/etc/xkeen-ui`;
- ставит init скрипт `/opt/etc/init.d/S99xkeen-ui` и запускает сервис;
- при первой установке делает бэкап конфигов Xray в `/opt/etc/xray/configs/backups`.

---

## 🌐 Доступ к панели
```text
http://<IP_роутера>:<порт>/
```

Лог панели: `/opt/var/log/xkeen-ui.log`

---

## 🧑‍💻 Управление сервисом через SSH
```sh
/opt/etc/init.d/S99xkeen-ui start
/opt/etc/init.d/S99xkeen-ui stop
/opt/etc/init.d/S99xkeen-ui restart
/opt/etc/init.d/S99xkeen-ui status
```

Удаление:
```sh
sh /opt/etc/xkeen-ui/uninstall.sh
```

---

## 🧰 Полезные переменные окружения

> Обычно удобнее задавать их в окружении сервиса (или через DevTools, если в сборке включён редактор env).

- `XKEEN_UI_STATE_DIR` — где хранить состояние UI (auth/secret/логи UI настроек)
- `XKEEN_UI_SECRET_KEY` — ключ сессий (если нужно фиксировать вручную)
- `XKEEN_ALLOW_SHELL` — разрешить произвольные команды в Command Runner (`1`/`0`)
- `XKEEN_XRAY_LOG_TZ_OFFSET` — смещение времени логов (часы), по умолчанию `3`

Файловый менеджер:
- `XKEEN_TRASH_DIR` — путь к корзине (local)
- `XKEEN_TRASH_MAX_GB` / `XKEEN_TRASH_MAX_BYTES` — лимит корзины
- `XKEEN_REMOTEFM_ENABLE` — включить Remote File Manager (`1`/`0`)
- `XKEEN_REMOTEFM_MAX_UPLOAD_MB` — лимит upload для remote
- `XKEEN_REMOTEFM_SESSION_TTL` — TTL remote сессий

---

## 🛠️ Troubleshooting

### Нет PTY/Live‑логов по WebSocket
- На некоторых архитектурах `gevent` может не собраться. В этом случае:
  - Live‑логи будут работать через **HTTP polling**,
  - PTY‑терминал может быть недоступен.

### Сбросить логин/пароль
```sh
/opt/etc/init.d/S99xkeen-ui stop
rm -f /opt/etc/xkeen-ui/auth.json
/opt/etc/init.d/S99xkeen-ui start
```

---

## 📝 Примечания
- Все фронтенд библиотеки (CodeMirror, xterm.js и т.д.) поставляются **локально** — панель не зависит от CDN.
- Проект ориентирован на Keenetic/Entware и аккуратно выбирает writable директории под состояние и временные файлы.

---

![xkeen-ui-demo](https://github.com/user-attachments/assets/f2f6649c-2e9b-49a2-9cd8-f855fbd55084)


